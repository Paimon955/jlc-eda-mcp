import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Setup（推荐：EDA 扩展 + websocat）","description":"","frontmatter":{},"headers":[],"relativePath":"SETUP.md","filePath":"SETUP.md","lastUpdated":1772158734000}');
const _sfc_main = { name: "SETUP.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="setup-推荐-eda-扩展-websocat" tabindex="-1">Setup（推荐：EDA 扩展 + websocat） <a class="header-anchor" href="#setup-推荐-eda-扩展-websocat" aria-label="Permalink to &quot;Setup（推荐：EDA 扩展 + websocat）&quot;">​</a></h1><p>本项目由两部分组成：</p><ul><li><strong>EDA 扩展</strong>：<code>packages/eda-extension</code>（运行在嘉立创 EDA Pro 内，执行真实的 <code>globalThis.eda.*</code> API；它是 <strong>WebSocket 客户端</strong>）</li><li><strong>本机 Bridge / WS 服务端</strong>：监听 <code>ws://127.0.0.1:&lt;port&gt;</code>（默认 <code>9050</code>），等待扩展连接，并向扩展发送 <code>docs/PROTOCOL.md</code> 定义的 <code>request</code></li></ul><blockquote><p>重要：EDA 扩展不会在 EDA 进程内监听 HTTP/TCP 端口；因此无论你是否使用 MCP（stdio）或 HTTP，都必须有一个本机 <strong>WS 服务端</strong> 让扩展连上。</p></blockquote><h2 id="a-推荐-websocat-无需-node-mcp" tabindex="-1">A) 推荐：websocat（无需 Node / MCP） <a class="header-anchor" href="#a-推荐-websocat-无需-node-mcp" aria-label="Permalink to &quot;A) 推荐：websocat（无需 Node / MCP）&quot;">​</a></h2><h3 id="_1-安装-websocat-通用-websocket-工具" tabindex="-1">1) 安装 websocat（通用 WebSocket 工具） <a class="header-anchor" href="#_1-安装-websocat-通用-websocket-工具" aria-label="Permalink to &quot;1) 安装 websocat（通用 WebSocket 工具）&quot;">​</a></h3><p>安装方式见仓库根 <code>README.md</code> 的 “安装 websocat” 小节。</p><h3 id="_2-安装-eda-扩展" tabindex="-1">2) 安装 EDA 扩展 <a class="header-anchor" href="#_2-安装-eda-扩展" aria-label="Permalink to &quot;2) 安装 EDA 扩展&quot;">​</a></h3><p>如果你已经有 <code>.eext</code>，直接在 EDA 扩展管理器里安装即可。</p><p>如果你在本仓库内从源码构建扩展：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -w</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> packages/eda-extension</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span></span></code></pre></div><p>生成的扩展包在：</p><ul><li><code>packages/eda-extension/build/dist/</code></li></ul><p>在嘉立创EDA 专业版客户端中安装该 <code>.eext</code>。</p><h3 id="_3-在-eda-内配置-ws-地址" tabindex="-1">3) 在 EDA 内配置 WS 地址 <a class="header-anchor" href="#_3-在-eda-内配置-ws-地址" aria-label="Permalink to &quot;3) 在 EDA 内配置 WS 地址&quot;">​</a></h3><p>安装后在 EDA 顶部菜单：</p><ul><li><code>MCP Bridge</code> -&gt; <code>Configure...</code>：填写 WebSocket URL：<code>ws://127.0.0.1:9050</code></li></ul><p>新版扩展默认 <strong>自动连接</strong>（必要时仍可用 <code>Connect/Disconnect</code> 强制触发）。</p><h3 id="_4-端到端验证-短驻-一次性调用" tabindex="-1">4) 端到端验证（短驻 / 一次性调用） <a class="header-anchor" href="#_4-端到端验证-短驻-一次性调用" aria-label="Permalink to &quot;4) 端到端验证（短驻 / 一次性调用）&quot;">​</a></h3><p>临时启动一个 WS 服务端，等待扩展连接，发 1 次请求并要求回包后断开：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">printf</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;%s\\n&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{&quot;type&quot;:&quot;request&quot;,&quot;id&quot;:&quot;1&quot;,&quot;method&quot;:&quot;ping&quot;,&quot;closeAfterResponse&quot;:true}&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> websocat</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -t</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --no-close</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --oneshot</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ws-l:127.0.0.1:9050</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> -</span></span></code></pre></div><p>输出会包含扩展的 <code>hello</code> 与本次 <code>response</code>。</p><p>（可选）验证 <code>jlc.*</code> tools（需要扩展支持 <code>tools.call</code>）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">printf</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;%s\\n&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{&quot;type&quot;:&quot;request&quot;,&quot;id&quot;:&quot;1&quot;,&quot;method&quot;:&quot;tools.call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;jlc.bridge.ping&quot;,&quot;arguments&quot;:{}},&quot;closeAfterResponse&quot;:true}&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> websocat</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -t</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --no-close</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --oneshot</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ws-l:127.0.0.1:9050</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> -</span></span></code></pre></div><h3 id="_5-避免-误用旧-mcp-server-占用端口-重要" tabindex="-1">5) 避免“误用旧 mcp-server”占用端口（重要） <a class="header-anchor" href="#_5-避免-误用旧-mcp-server-占用端口-重要" aria-label="Permalink to &quot;5) 避免“误用旧 mcp-server”占用端口（重要）&quot;">​</a></h3><p>短驻方案下，<code>9050</code> 端口应由 <strong>websocat</strong> 监听；如果被 <code>node.exe</code>（legacy <code>packages/mcp-server</code>）占用，会导致扩展无法连接。</p><p>Windows（PowerShell）检查：</p><div class="language-powershell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">powershell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">netstat </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">ano </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">|</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> findstr :</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">9050</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">tasklist </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">fi </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;pid eq &lt;PID&gt;&quot;</span></span></code></pre></div><h2 id="b-legacy-packages-mcp-server-node-计划废弃" tabindex="-1">B) Legacy：<code>packages/mcp-server</code>（Node，计划废弃） <a class="header-anchor" href="#b-legacy-packages-mcp-server-node-计划废弃" aria-label="Permalink to &quot;B) Legacy：\`packages/mcp-server\`（Node，计划废弃）&quot;">​</a></h2><p>如果你需要 MCP（stdio）或 legacy HTTP <code>/v1/*</code>（以及 <code>/docs</code> 静态入口），可以继续使用旧的 <code>packages/mcp-server</code>（不推荐作为默认工作流）。</p><p>构建：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">npm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -w</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> packages/mcp-server</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span></span></code></pre></div><p>启动（MCP stdio + WS Bridge）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> packages/mcp-server/dist/cli.js</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --port</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 9050</span></span></code></pre></div><p>启动（HTTP Proxy + WS Bridge；不给 MCP）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> packages/mcp-server/dist/cli.js</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --port</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 9050</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --http</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --no-mcp</span></span></code></pre></div><p>默认 HTTP：<code>http://127.0.0.1:9151</code>（<code>GET /v1/status</code>、<code>GET /v1/tools</code>、<code>POST /v1/tools/call</code>、<code>POST /v1/rpc</code>）。</p><p>（可选）Skills 文档静态入口（仅 legacy Node 方案提供）：</p><ul><li><code>http://127.0.0.1:9050/docs/</code></li></ul><h3 id="自测-legacy-node-方案" tabindex="-1">自测（legacy Node 方案） <a class="header-anchor" href="#自测-legacy-node-方案" aria-label="Permalink to &quot;自测（legacy Node 方案）&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> packages/mcp-server/dist/cli.js</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --port</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 9050</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --self-test</span></span></code></pre></div><p>自测会创建/打开一个原理图页并画一段测试导线，尽量尝试放置 0603 电阻并连线，然后执行 DRC + 保存，最后在终端输出 JSON 结果。</p><h2 id="自动化结果校验-推荐" tabindex="-1">自动化结果校验（推荐） <a class="header-anchor" href="#自动化结果校验-推荐" aria-label="Permalink to &quot;自动化结果校验（推荐）&quot;">​</a></h2><p>自动绘制/连线后，建议用以下工具做关键网络的连通性校验（见 <code>docs/VERIFY_NETS.md</code>）：</p><ul><li><code>jlc.schematic.verify_netlist</code>：优先（更接近真实网表，可覆盖跨网标/端口的逻辑连接）</li><li><code>jlc.schematic.verify_nets</code>：兜底（基于 <code>document source</code> 导线解析，不依赖网表 API）</li></ul><h2 id="权限提示-重要" tabindex="-1">权限提示（重要） <a class="header-anchor" href="#权限提示-重要" aria-label="Permalink to &quot;权限提示（重要）&quot;">​</a></h2><p>部分能力需要在 EDA 客户端里为扩展开启权限（否则会 <code>throw Error</code>）：</p><ul><li>WebSocket 连接（桥接到本机 WS 服务端：websocat 或 legacy mcp-server）：<code>SYS_WebSocket.register/send/close</code>（需要“外部交互”权限）</li><li>写文件到本地：<code>SYS_FileSystem.saveFileToFileSystem</code> / <code>SYS_FileSystem.getEdaPath</code></li><li>文档导出：<code>SYS_FileManager.getDocumentFile</code></li></ul><p>如果遇到 <code>EXPORT_FAILED</code> / <code>SAVE_FILE_FAILED</code> 等错误，请优先检查扩展权限是否已开启。</p><h2 id="自建最小-bridge-可选" tabindex="-1">自建最小 Bridge（可选） <a class="header-anchor" href="#自建最小-bridge-可选" aria-label="Permalink to &quot;自建最小 Bridge（可选）&quot;">​</a></h2><p>如果你不想使用 legacy <code>packages/mcp-server</code>（例如用户侧不方便使用 Node / 不需要 MCP），可以按 <code>docs/BRIDGE_QUICKSTART.md</code> 自建一个最小 Bridge：</p><ul><li>WebSocket 服务端：给 EDA 扩展连接（实现 <code>docs/PROTOCOL.md</code> 的 RPC）</li><li>（可选）HTTP 入口：方便用 <code>curl</code> 调用</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("SETUP.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const SETUP = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  SETUP as default
};
