import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"加速与稳定性（避免卡死 / 批处理）","description":"","frontmatter":{},"headers":[],"relativePath":"skills/jlceda-eda-rest/docs/04-performance.md","filePath":"skills/jlceda-eda-rest/docs/04-performance.md","lastUpdated":null}');
const _sfc_main = { name: "skills/jlceda-eda-rest/docs/04-performance.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="加速与稳定性-避免卡死-批处理" tabindex="-1">加速与稳定性（避免卡死 / 批处理） <a class="header-anchor" href="#加速与稳定性-避免卡死-批处理" aria-label="Permalink to &quot;加速与稳定性（避免卡死 / 批处理）&quot;">​</a></h1><blockquote><p>目标：让“读取/编辑原理图”既快又稳，减少 EDA 卡死与 DRC 的噪音报错。</p></blockquote><h2 id="调用侧-ws-加速" tabindex="-1">调用侧（WS）加速 <a class="header-anchor" href="#调用侧-ws-加速" aria-label="Permalink to &quot;调用侧（WS）加速&quot;">​</a></h2><ul><li>大结果优先用 RPC，少用 <code>tools.call</code>： <ul><li><code>tools.call</code> 会同时返回 <code>data</code> 与 <code>toolResult</code>（重复一份 payload），大图纸更容易超限/丢包</li><li>例如：读原理图优先 <code>schematic.listComponents/listWires/listTexts</code>，而不是 <code>tools.call</code> + <code>jlc.schematic.*</code></li></ul></li><li><code>websocat</code> 建议加大 buffer：<code>-B 10485760</code>（10MB），避免默认 64KB 导致的大消息异常</li><li>能并行就并行： <ul><li><code>schematic.listComponents</code> + <code>schematic.listWires</code> + <code>schematic.listTexts</code> 可并行请求（或对应 <code>jlc.schematic.*</code> tools）</li></ul></li><li>短驻建议“单次会话多步”：一次 <code>websocat</code> 会话发送多条 <code>request</code>，避免每一步都触发扩展重连 backoff（见 <code>05-http-proxy.md</code>）。</li><li>只读当前页：<code>schematic.listComponents { allSchematicPages:false }</code>（工具等价：<code>jlc.schematic.list_components</code>）</li><li>避免一次性拉太大对象：<code>jlc.eda.invoke</code> 建议配 <code>jsonSafe</code> 限制深度/数组长度</li></ul><h2 id="读取选区-最快的两轮策略-推荐" tabindex="-1">读取选区：最快的两轮策略（推荐） <a class="header-anchor" href="#读取选区-最快的两轮策略-推荐" aria-label="Permalink to &quot;读取选区：最快的两轮策略（推荐）&quot;">​</a></h2><blockquote><p>适用：用户说“读取当前选中区域/介绍模块功能”，尤其是多工程/多窗口时。</p></blockquote><p>（可选，更快更小）：如果当前页图元很多、你只关心选区，可直接用<br><code>eda.invoke sch_SelectControl.getAllSelectedPrimitives</code>（单次调用返回选区 mixed primitives；见 <code>docs/02-region-read.md</code>）。</p><ol><li>第 1 轮（快读，够用就停）： <ul><li><code>eda.invoke sch_SelectControl.getAllSelectedPrimitives_PrimitiveId</code></li><li><code>schematic.listComponents { allSchematicPages:false }</code></li><li><code>schematic.listTexts</code></li><li>本地用 <code>primitiveId ∈ selectedIds</code> 过滤，先产出 <strong>短摘要</strong>（关键器件/网名/电源/接口）</li></ul></li><li>第 2 轮（按需补连通性）： <ul><li>从第 1 轮筛出的网名里挑“关键 nets”</li><li><code>schematic.listWires { nets:[...] }</code>（只拉关键 nets 的导线，避免全量 wires）</li></ul></li></ol><p>多窗口建议（节省时间）：优先按 <code>../SKILL.md</code> 的“LLM 自动探测端口”脚本扫描 <code>9050-9059</code>，拿到每个窗口的 <code>hello.project</code> 后再逐个端口读取；不要长期挂后台（或反复起多轮扫端口），避免残留进程拖慢后续调用。</p><h2 id="绘制侧-apply-ir-稳定性" tabindex="-1">绘制侧（apply_ir）稳定性 <a class="header-anchor" href="#绘制侧-apply-ir-稳定性" aria-label="Permalink to &quot;绘制侧（apply_ir）稳定性&quot;">​</a></h2><ul><li><strong>批处理导线</strong>：先放器件/网标/文本，再把 wires 分批（例如每批 5~20 条）<code>apply_ir</code></li><li><strong>过滤 0 长度导线</strong>：<code>line=[x,y,x,y]</code> 这类会导致 create 失败</li><li><strong>不要写回内部网名</strong>：遇到 <code>$1N*</code>（或 <code>$1Nundefined</code>）建议省略 <code>net</code>，让 EDA 自动分配</li><li><strong>校验 UUID</strong>： <ul><li><code>deviceUuid/libraryUuid</code> 必须是 32 位 hex（不要把 primitive uuid/短 uuid 当 deviceUuid 用）</li><li>不确定就 <code>jlc.library.search_devices</code> 反查</li></ul></li></ul><h2 id="连接自检" tabindex="-1">连接自检 <a class="header-anchor" href="#连接自检" aria-label="Permalink to &quot;连接自检&quot;">​</a></h2><ul><li>状态（tools）：<code>{&quot;type&quot;:&quot;request&quot;,&quot;id&quot;:&quot;1&quot;,&quot;method&quot;:&quot;tools.call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;jlc.status&quot;,&quot;arguments&quot;:{}}}</code></li><li>连通（tools）：<code>{&quot;type&quot;:&quot;request&quot;,&quot;id&quot;:&quot;2&quot;,&quot;method&quot;:&quot;tools.call&quot;,&quot;params&quot;:{&quot;name&quot;:&quot;jlc.bridge.ping&quot;,&quot;arguments&quot;:{}}}</code></li><li>不连：通常是 EDA 扩展未连上 WS（检查扩展 <code>Configure...</code> 的 URL、权限，以及本机 <code>9050</code> 是否被旧进程占用）</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("skills/jlceda-eda-rest/docs/04-performance.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _04Performance = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _04Performance as default
};
