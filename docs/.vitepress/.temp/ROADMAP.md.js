import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Roadmap（绘制完整、美观、可迭代原理图所需工具规划）","description":"","frontmatter":{},"headers":[],"relativePath":"ROADMAP.md","filePath":"ROADMAP.md","lastUpdated":1766840491000}');
const _sfc_main = { name: "ROADMAP.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="roadmap-绘制完整、美观、可迭代原理图所需工具规划" tabindex="-1">Roadmap（绘制完整、美观、可迭代原理图所需工具规划） <a class="header-anchor" href="#roadmap-绘制完整、美观、可迭代原理图所需工具规划" aria-label="Permalink to &quot;Roadmap（绘制完整、美观、可迭代原理图所需工具规划）&quot;">​</a></h1><p>本文档描述 <code>jlc-eda-mcp</code> 在“能自动画原理图”基础上，向“完整 / 美观 / 可维护（多回合增量）”演进需要补齐的通用 MCP 工具与能力。</p><blockquote><p>原则：只做<strong>通用能力</strong>，不针对特定器件/板卡做特化；所有“画图结果”应可通过工具读回、验证、再增量修改。</p></blockquote><h2 id="现状-已具备的基础能力" tabindex="-1">现状（已具备的基础能力） <a class="header-anchor" href="#现状-已具备的基础能力" aria-label="Permalink to &quot;现状（已具备的基础能力）&quot;">​</a></h2><ul><li>器件库：<code>jlc.library.search_devices</code></li><li>放置/连线：<code>jlc.schematic.place_device</code>、<code>jlc.schematic.connect_pins</code>、<code>jlc.schematic.wire.create</code></li><li>增量绘制：<code>jlc.schematic.apply_ir</code>（upsert + 可选删除；见 <code>docs/SCHEMATIC_IR.md</code>）</li><li>读回/定位（P1 基础已就绪）： <ul><li><code>jlc.schematic.list_components</code> / <code>list_wires</code> / <code>list_texts</code></li><li><code>jlc.schematic.find_by_designator</code> / <code>select</code> / <code>crossprobe_select</code> / <code>zoom_to_all</code></li><li><code>jlc.schematic.snapshot</code>（结构化读回，支撑 LLM 多回合增量）</li></ul></li><li>网表/验证： <ul><li><code>jlc.schematic.get_netlist</code> / <code>jlc.schematic.export_netlist</code></li><li><code>jlc.schematic.verify_netlist</code>（优先 <code>SCH_Netlist.getNetlist</code>，超时/不支持时自动回退到导出网表文件再解析）</li><li><code>jlc.schematic.verify_nets</code>（基于 document source 的导线兜底校验；更适合纯导线连通验证）</li></ul></li><li>收尾：<code>jlc.schematic.drc</code>、<code>jlc.schematic.save</code>、<code>jlc.view.capture_png</code></li></ul><h2 id="设计目标-完整-美观-可用" tabindex="-1">设计目标（“完整 + 美观 + 可用”） <a class="header-anchor" href="#设计目标-完整-美观-可用" aria-label="Permalink to &quot;设计目标（“完整 + 美观 + 可用”）&quot;">​</a></h2><ol><li><strong>完整</strong>：电源地/端口/网标/连接点齐全；跨区域/跨线同名网络可靠。</li><li><strong>美观</strong>：布局规整、线条不乱、网标清晰、文本/注释规范，方便后续 PCB。</li><li><strong>可迭代</strong>：LLM 多回合增量更新时不会“迷路”，能定位已有对象并只改动必要部分。</li><li><strong>可验证</strong>：每一步都有可机读的验证（网表/DRC/截图），失败时能定位问题点。</li></ol><h2 id="规划-按优先级分阶段" tabindex="-1">规划（按优先级分阶段） <a class="header-anchor" href="#规划-按优先级分阶段" aria-label="Permalink to &quot;规划（按优先级分阶段）&quot;">​</a></h2><h3 id="p1-读回-定位-选择-增量绘制的地基" tabindex="-1">P1：读回/定位/选择（增量绘制的地基） <a class="header-anchor" href="#p1-读回-定位-选择-增量绘制的地基" aria-label="Permalink to &quot;P1：读回/定位/选择（增量绘制的地基）&quot;">​</a></h3><p>要解决的问题：仅靠 <code>apply_ir</code> 内部 map 记录仍不足以让外部（MCP）“知道当前画布上有什么、在哪里、叫什么”。</p><p>建议新增工具：</p><ul><li><code>jlc.schematic.list_components</code>：列出当前页组件（primitiveId、designator、deviceUuid、x/y/rotation 等）</li><li><code>jlc.schematic.list_wires</code> / <code>jlc.schematic.list_texts</code> / <code>jlc.schematic.list_netports</code> / <code>jlc.schematic.list_netflags</code></li><li><code>jlc.schematic.find_by_designator</code>：通过 <code>R1/U2/...</code> 查 primitiveId</li><li><code>jlc.schematic.select</code> / <code>jlc.schematic.highlight</code> / <code>jlc.schematic.zoom_to</code>：定位并可视化确认</li><li><code>jlc.schematic.snapshot</code>：导出结构化 JSON（用于 LLM “读回 -&gt; 决策 -&gt; 再画”）</li></ul><p>验收标准：</p><ul><li>任意一次 <code>apply_ir</code> 后，MCP 可以“列出/定位/高亮”刚刚创建或历史存在的对象。</li></ul><h3 id="p2-网标-连接点-端口-让逻辑连接稳定且可读" tabindex="-1">P2：网标/连接点/端口（让逻辑连接稳定且可读） <a class="header-anchor" href="#p2-网标-连接点-端口-让逻辑连接稳定且可读" aria-label="Permalink to &quot;P2：网标/连接点/端口（让逻辑连接稳定且可读）&quot;">​</a></h3><p>要解决的问题：靠“长导线”连一切会很丑；实际工程需要网标/端口/连接点让图可读。</p><p>建议新增/补齐能力：</p><ul><li>网标（NetLabel）创建/修改/删除：可指定 <code>net</code>、坐标、朝向，能贴到某根线段/某个点上</li><li>连接点（Junction）创建/删除：用于 T 形连接与可视化节点</li><li>端口（NetPort）与电源地标识（NetFlag）在 IR 中已支持，但需要补齐： <ul><li><code>snap</code> 能力：将导线端点精确落在网口/网标连接点上</li><li>失败可视化：验证失败时自动高亮相关对象/端点</li></ul></li></ul><p>验收标准：</p><ul><li>用 <code>verify_netlist</code> 能稳定验证跨线网标的同名网络连接（不依赖“连续导线”）。</li></ul><h3 id="p3-编辑与布局-把-能画-变成-画得漂亮" tabindex="-1">P3：编辑与布局（把“能画”变成“画得漂亮”） <a class="header-anchor" href="#p3-编辑与布局-把-能画-变成-画得漂亮" aria-label="Permalink to &quot;P3：编辑与布局（把“能画”变成“画得漂亮”）&quot;">​</a></h3><p>要解决的问题：自动放件/连线后需要重排：对齐、等距、避免线压器件/文字。</p><p>建议新增工具：</p><ul><li>基础变换：<code>move/rotate/mirror</code>（组件、文本、网标、端口、连接点）</li><li>批量布局：<code>align</code>（左/右/上/下/水平居中/垂直居中）、<code>distribute</code>（水平/垂直等距）</li><li>网格/吸附：<code>grid.get</code> / <code>grid.set</code> / <code>snap_to_grid</code></li><li>简易自动布线策略：Manhattan 走线 + 可配置 <code>midX</code>/折点；尽量避免穿过器件 bbox（先做启发式）</li></ul><p>验收标准：</p><ul><li>同一组元件可以被工具一键对齐/等距；导线折线风格一致；截图肉眼可读。</li></ul><h3 id="p4-属性-标注-可生产的工程信息" tabindex="-1">P4：属性/标注（可生产的工程信息） <a class="header-anchor" href="#p4-属性-标注-可生产的工程信息" aria-label="Permalink to &quot;P4：属性/标注（可生产的工程信息）&quot;">​</a></h3><p>要解决的问题：原理图不仅要“连对”，还要具备可生产信息（value、封装、BOM 字段）。</p><p>建议新增工具：</p><ul><li><code>jlc.schematic.annotate</code>：自动编号/去重（支持作用域：当前页/整工程）</li><li><code>jlc.schematic.set_properties</code>：设置/读取 value、comment、BOM 字段、自定义字段（如 LCSC、MPN）</li><li><code>jlc.schematic.swap_gate</code> / <code>swap_unit</code>（可选）：多单元器件重排</li></ul><p>验收标准：</p><ul><li>能通过工具统一设置/读取关键器件属性，并导出 BOM（若 API 支持）。</li></ul><h3 id="p5-多页与层级-可扩展的大工程" tabindex="-1">P5：多页与层级（可扩展的大工程） <a class="header-anchor" href="#p5-多页与层级-可扩展的大工程" aria-label="Permalink to &quot;P5：多页与层级（可扩展的大工程）&quot;">​</a></h3><p>要解决的问题：开发板/产品级电路往往多页；需要跨页网络与模块化结构。</p><p>建议新增能力：</p><ul><li>页管理：创建/切换/重命名/枚举 schematic pages</li><li>层级块（hierarchical sheet）与跨页端口策略（若 EDA API 支持）</li><li><code>snapshot</code>/<code>diff</code> 跨页：支持仅更新某一页或某一模块</li></ul><p>验收标准：</p><ul><li>MCP 能在指定页增量更新，不误创建新页；跨页网络可用 <code>verify_netlist</code> 验证。</li></ul><h3 id="p6-更强校验与交付-回归可控" tabindex="-1">P6：更强校验与交付（回归可控） <a class="header-anchor" href="#p6-更强校验与交付-回归可控" aria-label="Permalink to &quot;P6：更强校验与交付（回归可控）&quot;">​</a></h3><p>建议新增能力：</p><ul><li>DRC 结果明细读取（不仅 <code>ok</code>）：错误列表 + 自动定位/高亮</li><li>网表 diff：期望网络（Ref.Pin）与实际 netlist 的差异报告</li><li>导出：PDF/SVG（若 API 支持）、工程包、截图批量输出（用于审阅）</li></ul><p>验收标准：</p><ul><li>任意一次自动绘制后能输出：截图 + 校验报告 + 可复现的结构化 snapshot。</li></ul><h2 id="推荐实现顺序-最短路径" tabindex="-1">推荐实现顺序（最短路径） <a class="header-anchor" href="#推荐实现顺序-最短路径" aria-label="Permalink to &quot;推荐实现顺序（最短路径）&quot;">​</a></h2><ol><li>P1（读回/定位/选择）→ 2) P2（网标/连接点）→ 3) P3（布局）→ 4) 强化 <code>verify_netlist</code> 报告与高亮 → 再考虑 P4/P5/P6。</li></ol><p>这样可以最快把工作流从“工具能跑”升级到“每次增量都能对齐审阅与回归”。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("ROADMAP.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const ROADMAP = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  ROADMAP as default
};
